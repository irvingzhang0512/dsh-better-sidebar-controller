// One-off: inspect the built client bundle shape (run: node scripts/inspect-bundle.cjs)
const fs = require('node:fs')
const head = fs.readFileSync('lib/client.js', 'utf8')
const lines = head.split('\n')
console.log('=== first 3 lines ===')
console.log(lines.slice(0, 3).join('\n'))
console.log('=== last 4 lines ===')
console.log(lines.slice(-4).join('\n'))
const bad = /require\(["']node:|from ["']node:|process\.env/.test(head)
console.log('=== forbidden references ===')
console.log(bad ? 'FOUND (bad)' : 'none (good)')
console.log('=== bundle id present ===')
console.log(head.includes('dsh-better-sidebar-controller') ? 'yes' : 'no')
console.log('=== exports injected ===')
console.log('exports.apply present:', /exports\.apply/.test(head))
console.log('exports.inject present:', /exports\.inject/.test(head))
